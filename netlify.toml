[build]
  command = "npm run build"
  publish = "dist"

# Security headers for production
[[headers]]
  for = "/*"
  [headers.values]
    # Content Security Policy - prevents XSS attacks
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' cdn.jsdelivr.net; font-src 'self' fonts.googleapis.com fonts.gstatic.com cdn.jsdelivr.net; img-src 'self' data: https:; connect-src 'self' api.emailjs.com; frame-ancestors 'none';"
    Content-Security-Policy = "default-src 'self'; style-src 'self' 'unsafe-inline' cdn.jsdelivr.net https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self' 'unsafe-inline' 'unsafe-eval'; connect-src 'self' https://api.emailjs.com; img-src 'self' data: blob:;"
    
    # Prevent MIME type sniffing
    X-Content-Type-Options = "nosniff"
    
    # Clickjacking protection
    X-Frame-Options = "DENY"
    
    # XSS protection for older browsers
    X-XSS-Protection = "1; mode=block"
    
    # Referrer policy
    Referrer-Policy = "strict-origin-when-cross-origin"
    
    # Permissions policy - disable unnecessary browser features
    Permissions-Policy = "geolocation=(), microphone=(), camera=(), payment=()"
    
    # HTTPS enforcement
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# Redirect rules for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
